<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-gear-fill"></i> Cấu Hình Hệ Thống</h2>
        <div>
            <a href="/admin" class="btn btn-outline-secondary me-2">
                <i class="bi bi-speedometer2"></i> Dashboard
            </a>
            <a href="/admin/users" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Quay lại Admin
            </a>
        </div>
    </div>

    <div class="alert alert-info">
        <i class="bi bi-info-circle"></i>
        <strong>Lưu ý:</strong> Các thay đổi cấu hình sẽ áp dụng ngay lập tức cho toàn bộ hệ thống.
    </div>

    <form method="POST" action="/admin/config/update">
        <!-- Auction Settings -->
        <div class="card mb-4 shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0"><i class="bi bi-hammer"></i> Cài Đặt Đấu Giá</h5>
            </div>
            <div class="card-body">
                {{#each groupedConfigs.auction}}
                <div class="mb-3 row">
                    <label class="col-sm-4 col-form-label">
                        <strong>{{this.config_key}}</strong>
                        <br>
                        <small class="text-muted">{{this.description}}</small>
                    </label>
                    <div class="col-sm-4">
                        <input type="number" 
                               name="config_{{this.config_key}}" 
                               class="form-control" 
                               value="{{this.config_value}}"
                               min="0"
                               step="{{#if (or (isEqual this.config_key 'rating_threshold_percent'))}}0.1{{else}}1{{/if}}"
                               required>
                    </div>
                    <div class="col-sm-4">
                        <span class="badge bg-secondary">
                            
                        </span>
                        <small class="text-muted">
                            Hiện tại: <strong>{{this.config_value}}</strong>
                        </small>
                    </div>
                </div>
                <hr>
                {{/each}}
            </div>
        </div>

        <!-- Upload Settings -->
        <div class="card mb-4 shadow-sm">
            <div class="card-header bg-success text-white">
                <h5 class="mb-0"><i class="bi bi-image"></i> Cài Đặt Upload Hình Ảnh</h5>
            </div>
            <div class="card-body">
                {{#each groupedConfigs.upload}}
                <div class="mb-3 row">
                    <label class="col-sm-4 col-form-label">
                        <strong>{{this.config_key}}</strong>
                        <br>
                        <small class="text-muted">{{this.description}}</small>
                    </label>
                    <div class="col-sm-4">
                        <input type="number" 
                               name="config_{{this.config_key}}" 
                               class="form-control" 
                               value="{{this.config_value}}"
                               min="1"
                               max="20"
                               required>
                    </div>
                    <div class="col-sm-4">
                        <span class="badge bg-secondary">
                            
                        </span>
                        <small class="text-muted">
                            Hiện tại: <strong>{{this.config_value}}</strong>
                        </small>
                    </div>
                </div>
                <hr>
                {{/each}}
            </div>
        </div>

        <!-- System Settings -->
        <div class="card mb-4 shadow-sm">
            <div class="card-header bg-info text-white">
                <h5 class="mb-0"><i class="bi bi-server"></i> Cài Đặt Hệ Thống</h5>
            </div>
            <div class="card-body">
                {{#each groupedConfigs.system}}
                <div class="mb-3 row">
                    <label class="col-sm-4 col-form-label">
                        <strong>{{this.config_key}}</strong>
                        <br>
                        <small class="text-muted">{{this.description}}</small>
                    </label>
                    <div class="col-sm-4">
                        <input type="number" 
                               name="config_{{this.config_key}}" 
                               class="form-control" 
                               value="{{this.config_value}}"
                               min="1"
                               required>
                    </div>
                    <div class="col-sm-4">
                        <span class="badge bg-secondary">
                            
                        </span>
                        <small class="text-muted">
                            Hiện tại: <strong>{{this.config_value}}</strong>
                        </small>
                    </div>
                </div>
                <hr>
                {{/each}}
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="d-flex justify-content-between">
            <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#resetModal">
                <i class="bi bi-arrow-clockwise"></i> Khôi Phục Mặc Định
            </button>
            <button type="submit" class="btn btn-primary btn-lg">
                <i class="bi bi-save"></i> Lưu Cấu Hình
            </button>
        </div>
    </form>
</div>

<!-- Reset Confirmation Modal -->
<div class="modal fade" id="resetModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-warning">
                <h5 class="modal-title">
                    <i class="bi bi-exclamation-triangle"></i> Xác Nhận Khôi Phục
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>Bạn có chắc chắn muốn khôi phục tất cả cấu hình về giá trị mặc định không?</p>
                <p class="text-danger mb-0">
                    <strong>Lưu ý:</strong> Hành động này không thể hoàn tác!
                </p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                <form method="POST" action="/admin/config/reset" style="display: inline;">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-arrow-clockwise"></i> Khôi Phục
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

{{#section 'js'}}
<script>
    console.log('[ADMIN CONFIG] Toast script loaded');
    console.log('[ADMIN CONFIG] showToast function exists:', typeof showToast);
    
    {{#if successMessage}}
    console.log('[ADMIN CONFIG] Success message:', {{{json successMessage}}});
    if (typeof showToast === 'function') {
        showToast({{{json successMessage}}}, 'success');
    } else {
        console.error('[ADMIN CONFIG] showToast function not found!');
    }
    {{else}}
    console.log('[ADMIN CONFIG] No success message');
    {{/if}}
    
    {{#if errorMessage}}
    console.log('[ADMIN CONFIG] Error message:', {{{json errorMessage}}});
    if (typeof showToast === 'function') {
        showToast({{{json errorMessage}}}, 'error');
    } else {
        console.error('[ADMIN CONFIG] showToast function not found!');
    }
    {{else}}
    console.log('[ADMIN CONFIG] No error message');
    {{/if}}
</script>
{{/section}}

<style>
.card-header h5 {
    margin: 0;
}

.row.mb-3:last-child hr {
    display: none;
}

.badge {
    font-size: 0.75rem;
}
</style>
