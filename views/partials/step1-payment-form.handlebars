<div class="card">
    <div class="card-header bg-primary text-white">
        <h5>
            <i class="bi bi-1-circle"></i> Bước 1: Thanh toán
            <i class="bi bi-info-circle ms-2" data-bs-toggle="tooltip" data-bs-placement="top" 
               title="Upload hoá đơn chuyển khoản và địa chỉ nhận hàng"></i>
        </h5>
    </div>
    <div class="card-body">
        <form method="POST" action="/transaction/{{transaction.id}}/step1" enctype="multipart/form-data" id="step1Form">
            <div class="mb-3">
                <label class="form-label">
                    Địa chỉ giao hàng 
                    <span class="text-danger">*</span>
                </label>
                <textarea name="shipping_address" class="form-control" rows="3" required
                    placeholder="Số nhà, đường, phường/xã, quận/huyện, tỉnh/thành phố"
                    minlength="20"></textarea>
                <small class="text-muted">
                    <i class="bi bi-lightbulb"></i>
                    Vui lòng cung cấp địa chỉ đầy đủ để người bán gửi hàng
                </small>
            </div>
            
            <div class="mb-3">
                <label class="form-label">
                    Hoá đơn thanh toán 
                    <span class="text-danger">*</span>
                </label>
                <input type="file" name="payment_proof" class="form-control" 
                    accept="image/*,.pdf" required>
                <small class="text-muted">
                    <i class="bi bi-file-earmark-image"></i>
                    JPG, PNG hoặc PDF, tối đa 5MB
                </small>
            </div>

            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle"></i>
                Vui lòng chuyển khoản <strong>{{format_currency transaction.final_price}} VNĐ</strong> 
                đến tài khoản của người bán và upload ảnh chụp biên lai.
            </div>

            <button type="submit" class="btn btn-primary w-100" id="submitBtn">
                <i class="bi bi-upload"></i> Upload và Tiếp tục
            </button>
        </form>
    </div>
</div>
