<div class="card shadow-sm">
    <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
        <h5 class="mb-0 text-warning">
            <i class="bi bi-person-up me-2"></i> Danh sách Yêu cầu Nâng cấp (Bidder ➝ Seller)
        </h5>
        <a href="/admin/users" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left"></i> Quay lại danh sách User
        </a>
    </div>

    <div class="card-body">
        {{#if empty}}
            <div class="text-center py-5">
                <i class="bi bi-check-circle-fill text-success display-1"></i>
                <p class="mt-3 fs-5 text-muted">Tuyệt vời! Không có yêu cầu nào đang chờ duyệt.</p>
            </div>
        {{else}}
            <div class="table-responsive">
                <table class="table table-hover table-bordered align-middle">
                    <thead class="table-light text-center">
                        <tr>
                            <th style="width: 50px;">#ID</th>
                            <th>Thông tin người dùng</th>
                            <th>Email</th>
                            <th>Thời gian gửi</th>
                            <th style="width: 200px;">Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each requests}}
                        <tr>
                            <td class="text-center fw-bold text-muted">{{request_id}}</td>
                            
                            <td>
                                <a href="/admin/users/detail/{{id}}" target="_blank" class="fw-bold text-decoration-none text-dark">
                                    {{full_name}} <i class="bi bi-box-arrow-up-right small text-muted"></i>
                                </a>
                                <br>
                                <small class="text-muted">User ID: {{id}}</small>
                            </td>

                            <td>{{email}}</td>

                            <td class="text-center">
                                <span class="badge bg-light text-dark border">
                                    <i class="bi bi-clock"></i> {{request_time}}
                                </span>
                            </td>

                            <td class="text-center">
                                <div class="d-flex justify-content-center gap-2">
                                    
                                    <form method="post" action="/admin/users/approve">
                                        <input type="hidden" name="requestId" value="{{request_id}}">
                                        <input type="hidden" name="userId" value="{{id}}">
                                        <button type="submit" class="btn btn-success btn-sm" title="Chấp thuận nâng cấp">
                                            <i class="bi bi-check-lg"></i> Duyệt
                                        </button>
                                    </form>

                                    <form method="post" action="/admin/users/reject" onsubmit="return confirm('Bạn chắc chắn muốn từ chối yêu cầu này?');">
                                        <input type="hidden" name="requestId" value="{{request_id}}">
                                        <button type="submit" class="btn btn-danger btn-sm" title="Từ chối yêu cầu">
                                            <i class="bi bi-x-lg"></i> Từ chối
                                        </button>
                                    </form>

                                </div>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        {{/if}}
    </div>
    
    <div class="card-footer bg-white text-muted small">
        <i class="bi bi-info-circle-fill"></i> Lưu ý: Khi duyệt, người dùng sẽ lập tức có quyền đăng bán sản phẩm (Role: Seller).
    </div>
</div>